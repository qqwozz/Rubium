# Задачи на октябрь

## Илья

1. Доделать `sRFTM`(получить данные из `DATA_TASK.md`, сменить принцип модели на паттерновый)
2. Сделать фронтенд логику кооп режима тетрадей(`git` аналогия, push, diffs, merge logic)
3. Сделать `database` логику кооп режима тетрадей(добавить новые колонки, сделать `таблицы связей`)
4. Реализовать механику `fork`(`database` + `frontend`)
5. Перевести процесс `логина/авторизации` на `Go`
6. Интегрировать **рекомендательную систему**(`Go-endpoint`)
7. Механика прикрепления файлов к тетради
8. **Экспорт** в разные форматы
9. **Импорт** из разных форматов(Сложно сейчас, полезно в будущем)

Документы для большей информации:

1. `Version_control_ideas.md`
2. `RECOMMENDATION_MATH.md`
3. [sRFTM](https://github.com/nsdmljk/sRFTM)

---

## Дима

1. Завершить интеграцию `OAuth`
2. Перевести эндпоинты `логина/ренистрации` из `JS`на`Go`
3. Отредактировать эндпоинты каталога для интеграции **рекомендательной системы**(см. `api/CURRENT_TASK2.md`)
4. Написать эндпоинт генерации `sitemap.xml`(см. `api/CURRENT_TASK.md`)
5. Продумать и реализовать `backend` механики **системы контроля версий**(для п.2-4 Илья)
6. *Допиши свои если я не всё написал*

---

## Олег

1. Напиши свои таски

---

## Антон

1. Собрать данные для `п.1 Илья`(см. `DATA_TASK.md`)
2. Довести бота до хорошего состояния(интеграция с Python сервисом рекомендации и Go API)
